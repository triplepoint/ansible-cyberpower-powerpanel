[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
molecule = {extras = ["docker", "lint", "test"], version = "^25.6.0"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
